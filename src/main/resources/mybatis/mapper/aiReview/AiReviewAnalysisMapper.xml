<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.ai.AiReviewAnalysisDao">

  <select id="selectAiReviewAnalysisById" resultType="com.example.demo.dto.ai.AiReviewAnalysis">
    SELECT id, prompt_text, input_json, output_json, created_at, user_id, review_post_id
    FROM ai_review_analysis WHERE id = #{id}
  </select>

  <insert id="insertAiReviewAnalysis" parameterType="com.example.demo.dto.ai.AiReviewAnalysis" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    INSERT INTO ai_review_analysis (prompt_text, input_json, output_json, created_at, user_id, review_post_id)
    VALUES (#{promptText}, #{inputJson}, #{outputJson}, now(), #{userId}, #{reviewPostId})
  </insert>

  <update id="updateAiReviewAnalysis" parameterType="com.example.demo.dto.ai.AiReviewAnalysis">
    UPDATE ai_review_analysis
    SET prompt_text = #{promptText}, input_json = #{inputJson}, output_json = #{outputJson},
        created_at = #{createdAt}, user_id = #{userId}, review_post_id = #{reviewPostId}
    WHERE id = #{id}
  </update>

  <delete id="deleteAiReviewAnalysisById" parameterType="long">
    DELETE FROM ai_review_analysis WHERE id = #{id}
  </delete>

</mapper>
