<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.travelgram.aiReview.dao.AiReviewAnalysisDao">

  <insert id="insertAiReviewAnalysis" parameterType="AiReviewAnalysis" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    INSERT INTO ai_review_analysis (input_json, output_json, created_at, user_id, review_post_id)
    VALUES (#{inputJson}, #{outputJson}, now(), #{userId}, #{reviewPostId})
  </insert>

  <select id="selectAiReviewAnalysisById" resultType="AiReviewAnalysis">
    SELECT input_json, output_json, created_at, user_id, review_post_id
    FROM ai_review_analysis WHERE id = #{id}
  </select>


  <update id="updateAiReviewAnalysis" parameterType="AiReviewAnalysis">
    UPDATE ai_review_analysis
    SET input_json = #{inputJson}, output_json = #{outputJson},
        created_at = #{createdAt}, user_id = #{userId}, review_post_id = #{reviewPostId}
    WHERE id = #{id}
  </update>

  <delete id="deleteAiReviewAnalysisById" parameterType="long">
    DELETE FROM ai_review_analysis WHERE id = #{id}
  </delete>

</mapper>
