<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.reviewHashtag.ReviewHashtagDao">

  <select id="selectHashtagById" resultType="com.example.demo.dto.hashtag.Hashtag">
    SELECT id, name, is_selected, created_at, group_id
    FROM review_hastags WHERE id = #{id}
  </select>

  <insert id="insertHashtag" parameterType="com.example.demo.dto.hashtag.Hashtag" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    INSERT INTO review_hastags (name, is_selected, created_at, group_id)
    VALUES (#{name}, #{isSelected}, #{createdAt}, #{groupId})
  </insert>

  <update id="updateHashtag" parameterType="com.example.demo.dto.hashtag.Hashtag">
    UPDATE review_hastags
    SET name = #{name}, is_selected = #{isSelected}, created_at = #{createdAt}, group_id = #{groupId}
    WHERE id = #{id}
  </update>

  <delete id="deleteHashtagById" parameterType="long">
    DELETE FROM review_hastags WHERE id = #{id}
  </delete>

</mapper>
