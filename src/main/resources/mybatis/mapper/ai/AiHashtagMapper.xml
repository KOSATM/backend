<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.ai.AiHashtagMapper">

  <select id="selectAiHashtagById" resultType="com.example.demo.dto.ai.AiHashtag">
    SELECT id, name, created_at, review_analysis_id
    FROM ai_hashtag WHERE id = #{id}
  </select>

  <insert id="insertAiHashtag" parameterType="com.example.demo.dto.ai.AiHashtag" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    INSERT INTO ai_hashtag (name, created_at, review_analysis_id)
    VALUES (#{name}, #{createdAt}, #{reviewAnalysisId})
  </insert>

  <update id="updateAiHashtag" parameterType="com.example.demo.dto.ai.AiHashtag">
    UPDATE ai_hashtag
    SET name = #{name}, created_at = #{createdAt}, review_analysis_id = #{reviewAnalysisId}
    WHERE id = #{id}
  </update>

  <delete id="deleteAiHashtagById" parameterType="long">
    DELETE FROM ai_hashtag WHERE id = #{id}
  </delete>

</mapper>
