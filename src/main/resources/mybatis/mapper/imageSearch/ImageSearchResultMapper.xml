<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.imageSearch.ImageSearchResultMapper">

  <select id="selectImageSearchResultById" resultType="com.example.demo.dto.imageSearch.ImageSearchResult">
    SELECT id, image_search_place_id, place_id, is_selected, rank
    FROM image_search_results WHERE id = #{id}
  </select>

  <insert id="insertImageSearchResult" parameterType="com.example.demo.dto.imageSearch.ImageSearchResult" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    INSERT INTO image_search_results (image_search_place_id, place_id, is_selected, rank)
    VALUES (#{imageSearchPlaceId}, #{placeId}, #{isSelected}, #{rank})
  </insert>

  <update id="updateImageSearchResult" parameterType="com.example.demo.dto.imageSearch.ImageSearchResult">
    UPDATE image_search_results
    SET image_search_place_id = #{imageSearchPlaceId}, place_id = #{placeId}, is_selected = #{isSelected}, rank = #{rank}
    WHERE id = #{id}
  </update>

  <delete id="deleteImagePlaceSearchResultById" parameterType="long">
    DELETE FROM image_search_results WHERE id = #{id}
  </delete>

</mapper>
