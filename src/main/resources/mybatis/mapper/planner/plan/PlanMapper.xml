<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.planner.plan.dao.PlanDao">

  <select id="selectPlanById" parameterType="long" resultType="Plan">
    SELECT id, user_id, budget, start_date, end_date, created_at, updated_at, is_ended, title
    FROM plans
    WHERE id = #{id}
  </select>

  <select id="selectPlansByUserId" parameterType="long" resultType="Plan">
    SELECT id, user_id, budget, start_date, end_date, created_at, updated_at, is_ended, title
    FROM plans
    WHERE user_id = #{userId}
    ORDER BY created_at DESC
  </select>

  <select id="selectActiveTravelPlanByUserId" parameterType="long" resultType="Plan">
    SELECT id, user_id, budget, start_date, end_date, created_at, updated_at, is_ended, title
    FROM plans
    WHERE user_id = #{userId} AND (is_ended = false OR is_ended IS NULL)
    ORDER BY created_at DESC
    LIMIT 1
  </select>

  <select id="selectCompletedTravelPlansByUserId" parameterType="long" resultType="Plan">
    SELECT id, user_id, budget, start_date, end_date, created_at, updated_at, is_ended, title
    FROM plans
    WHERE user_id = #{userId} AND is_ended = true
    ORDER BY created_at DESC
  </select>

  <select id="selectAllTravelPlans" resultType="Plan">
    SELECT id, user_id, budget, start_date, end_date, created_at, updated_at, is_ended, title
    FROM plans
    ORDER BY created_at DESC
  </select>

  <insert id="insertPlan" parameterType="Plan" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    INSERT INTO plans (user_id, budget, start_date, end_date, created_at, updated_at, is_ended, title)
    VALUES (#{userId}, #{budget}, #{startDate}, #{endDate}, now(), now(), #{isEnded}, #{title})
  </insert>

  <update id="updatePlan" parameterType="Plan">
    UPDATE plans
    SET user_id = #{userId},
        budget = #{budget},
        start_date = #{startDate},
        end_date = #{endDate},
        updated_at = now(),
        is_ended = #{isEnded},
        title = #{title}
    WHERE id = #{id}
  </update>

  <update id="updatePlanTitleById" parameterType="Plan">
    UPDATE plans
    SET title = #{title}
    WHERE id = #{id}
  </update>

  <update id="updatePlanDates">
    UPDATE plans
    SET start_date = #{startDate},
        end_date = #{endDate},
        updated_at = now()
    WHERE id = #{planId}
  </update>

  <delete id="deletePlan" parameterType="long">
    DELETE FROM plans
    WHERE id = #{id}
  </delete>

  <select id="selectEndedPlansWithNoTitle" resultType="Plan">
        SELECT id, user_id, budget, start_date, end_date, created_at, updated_at, is_ended, title
        FROM plans
        WHERE is_ended = true 
          AND (title IS NULL OR title = '')
        ORDER BY created_at DESC
    </select>
</mapper>
