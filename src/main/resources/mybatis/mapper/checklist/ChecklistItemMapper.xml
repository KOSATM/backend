<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.checklist.ChecklistItemDao">

  <select id="selectChecklistItemById" resultType="com.example.demo.dto.checklist.ChecklistItem">
    SELECT id, checklist_id, content, category, is_checked, created_at
    FROM checklist_items WHERE id = #{id}
  </select>

  <insert id="insertChecklistItem" parameterType="com.example.demo.dto.checklist.ChecklistItem" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    INSERT INTO checklist_items (checklist_id, content, category, is_checked, created_at)
    VALUES (#{checklistId}, #{content}, #{category}, #{isChecked}, now())
  </insert>

  <update id="updateChecklistItem" parameterType="com.example.demo.dto.checklist.ChecklistItem">
    UPDATE checklist_items
    SET checklist_id = #{checklistId}, content = #{content}, category = #{category}, is_checked = #{isChecked}, created_at = #{createdAt}
    WHERE id = #{id}
  </update>

  <delete id="deleteChecklistItemById" parameterType="long">
    DELETE FROM checklist_items WHERE id = #{id}
  </delete>

</mapper>
