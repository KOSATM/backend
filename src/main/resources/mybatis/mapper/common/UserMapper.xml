<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"\>

<mapper namespace="com.example.demo.common.user.dao.UserDao">

    <insert id="insertUser" parameterType="com.example.demo.common.dto.user.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (is_active, last_login_at, name, email, profile_image_url, created_at, updated_at)
        VALUES (#{isActive}, #{lastLoginAt}, #{name}, #{email}, #{profileImageUrl}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="selectUserById" parameterType="long" resultType="com.example.demo.common.dto.user.User">
        SELECT id, is_active, last_login_at, name, email, profile_image_url, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="selectAllUsers" resultType="com.example.demo.common.dto.user.User">
        SELECT id, is_active, last_login_at, name, email, profile_image_url, created_at, updated_at
        FROM users
        ORDER BY created_at DESC
    </select>

    <update id="updateUser" parameterType="com.example.demo.common.dto.user.User">
        UPDATE users
        SET is_active = #{isActive},
            last_login_at = #{lastLoginAt},
            name = #{name},
            email = #{email},
            profile_image_url = #{profileImageUrl},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteUser" parameterType="long">
        DELETE FROM users
        WHERE id = #{id}
    </delete>

    <select id="selectUserByEmail" parameterType="string" resultType="com.example.demo.common.dto.user.User">
        SELECT id, is_active, last_login_at, name, email, profile_image_url, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

</mapper>
